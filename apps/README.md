# v11 Applications

This folder keeps user-facing apps separate from the cloud backend:

- `decision-hub`: local operator console.
- `desktop-cloud-os`: Windows desktop shell and packaging.
- `trade-platform`: customer portal and intake front end.

The backend remains the source of truth for search, AI decisions, approval gates,
reports, audit memory, licensing, and cloud acceptance.
