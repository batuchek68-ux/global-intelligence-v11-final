# Copy to cloud.env.ps1 for local use. Do not commit cloud.env.ps1.
$env:GITHUB_REPOSITORY = "owner/repository"

# Keep the token out of files when possible:
# $env:GITHUB_TOKEN = "your GitHub token"

